<!Doctype html>
<html lang="en">
<head>
  <style>
    div {
      background-color: lightblue;
      width: 500px;

      border: 10px blue;
      padding: 5px;
      margin: 10px;
    }

    table, th, td {
      border: 1px solid black;
      cellspacing: 0px solid black;
      cellpadding: 0px;
    }
  </style>
</head>
<body>
<center>
<div>
<h1> PHOTOSITE User Index Page </h1>
<h2> List Of Users</h2>
</div>
<table>

    <tr>
      <th> User Id</th>
      <th> First Name</th>
      <th> Last Name</th>
    </tr>

  <tbody>
  <% @users.each do |user| %>
  <tr>
    <th><%= link_to user.id, user_photos_path(user.id) %></th>
    <td><%= link_to user.first_name, user_photos_path(user.id) %></td>
    <td><%= link_to user.last_name, user_photos_path(user.id) %></td>
  </tr>
      <%end%>
  </tbody>
</table>
</center>
</body>
</html>
